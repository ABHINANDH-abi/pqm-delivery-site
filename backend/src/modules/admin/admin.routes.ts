import { Router } from 'express';

const router = Router();

// TODO (Stage 3+): Implement admin endpoints
// GET  /api/v1/admin/dashboard          (stats summary)
// GET  /api/v1/admin/orders             (all orders with filters)
// GET  /api/v1/admin/customers          (customer list)
// GET  /api/v1/admin/delivery-partners  (delivery partner list)
// POST /api/v1/admin/delivery-partners  (register delivery partner)

export default router;
